import type { WorkerOptions, WorkerParseResult, WorkerProcessResult } from './types.js';
export declare const performSyncWork: ((options: Omit<WorkerOptions, "process">) => WorkerParseResult) & ((options: WorkerOptions & {
    process: true;
}) => WorkerProcessResult);
