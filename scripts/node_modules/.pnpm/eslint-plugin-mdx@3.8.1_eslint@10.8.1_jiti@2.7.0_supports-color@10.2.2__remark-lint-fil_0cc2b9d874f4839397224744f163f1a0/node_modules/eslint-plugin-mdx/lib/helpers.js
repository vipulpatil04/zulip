import { createRequire } from 'node:module';
export const getGlobals = (sources, initialGlobals = {}) => (Array.isArray(sources)
    ?
        sources
    : Object.keys(sources)).reduce((globals, source) => Object.assign(globals, {
    [source]: false,
}), initialGlobals);
const importMetaUrl = import.meta.url;
export const cjsRequire = importMetaUrl
    ? createRequire(importMetaUrl)
    : require;
//# sourceMappingURL=helpers.js.map